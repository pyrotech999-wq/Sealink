# Copy to keystore.properties (same folder) and fill in. Do not commit keystore.properties or .jks files.
# Paths are relative to the android/ directory unless you use an absolute path.

storeFile=sealink-upload.jks
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
