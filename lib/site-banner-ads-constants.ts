/**
 * Dock MOB button (`ManOverboardAlertButton` variant dock) is ~3rem tall (py-3 + text).
 * Banner image max height is derived from that (slim strip), then shortened a further 20%.
 */
export const SITE_BANNER_MOB_BUTTON_REM = 3;
/** Slim cap vs MOB dock, then 20% shorter sponsor image height. */
export const SITE_BANNER_MAX_HEIGHT_REM = SITE_BANNER_MOB_BUTTON_REM * 1.5 * 0.7 * 0.8;
