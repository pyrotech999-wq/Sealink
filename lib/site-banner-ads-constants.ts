/**
 * Dock MOB button (`ManOverboardAlertButton` variant dock) is ~3rem tall (py-3 + text).
 * Banner image max height = 1.5× that (per product spec).
 */
export const SITE_BANNER_MOB_BUTTON_REM = 3;
/** Reduced by ~30% to keep the bottom dock slimmer. */
export const SITE_BANNER_MAX_HEIGHT_REM = SITE_BANNER_MOB_BUTTON_REM * 1.5 * 0.7;
