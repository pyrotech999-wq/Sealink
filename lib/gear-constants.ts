/** Listing lifetime (approx. two months). Override on server with GEAR_LISTING_TTL_DAYS. */
export const GEAR_LISTING_TTL_DAYS = 60;

/** Days before deletion when reminders appear. Override with GEAR_REMINDER_DAYS_BEFORE. */
export const GEAR_REMINDER_DAYS_BEFORE = 7;

export const GEAR_SELLER_COOKIE = "sealink_gear_seller";

/** Max age for seller cookie (~13 months). */
export const GEAR_SELLER_COOKIE_MAX_AGE = 60 * 60 * 24 * 400;
